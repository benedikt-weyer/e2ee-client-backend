name: Release npm Package

on:
  workflow_dispatch:
    inputs:
      bump:
        description: Semantic version increment to apply from the latest tag
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write
  id-token: write

concurrency:
  group: e2ee-client-backend-release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      HAS_NPM_TOKEN: ${{ secrets.NPM_TOKEN != '' }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.6.5

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Compute next release version
        id: version
        run: |
          next_version="$(./scripts/next-version.sh "${{ inputs.bump }}")"
          echo "version=$next_version" >> "$GITHUB_OUTPUT"
          echo "tag=v$next_version" >> "$GITHUB_OUTPUT"

      - name: Configure Git author
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Update package version
        run: npm version "${{ steps.version.outputs.version }}" --no-git-tag-version

      - name: Publish to npmjs with Trusted Publishing
        id: publish_oidc
        continue-on-error: true
        env:
          NODE_AUTH_TOKEN: ""
          NPM_TOKEN: ""
          NPM_PUBLISH_PREFER_TOKEN: "false"
        run: ./scripts/publish-npm.sh

      - name: Publish to npmjs with NPM_TOKEN fallback
        id: publish_token
        if: steps.publish_oidc.outcome == 'failure' && env.HAS_NPM_TOKEN == 'true'
        env:
          NPM_PUBLISH_PREFER_TOKEN: "true"
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: ./scripts/publish-npm.sh

      - name: Fail if publish did not succeed
        if: steps.publish_oidc.outcome == 'failure' && (env.HAS_NPM_TOKEN != 'true' || steps.publish_token.outcome == 'failure')
        run: exit 1

      - name: Commit release version
        run: |
          git add package.json
          git commit -m "chore: release ${{ steps.version.outputs.tag }}"

      - name: Create git tag
        run: git tag "${{ steps.version.outputs.tag }}"

      - name: Push release commit and tag
        run: |
          git push origin "HEAD:${GITHUB_REF_NAME}"
          git push origin "${{ steps.version.outputs.tag }}"